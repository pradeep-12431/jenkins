pipeline {
    agent {
    label 'linux'
}

    stages {
        stage('Hello') {
            steps {
                echo 'Hello from Jenkins!'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Successful!!!!!'
            }
        }
    }
}
